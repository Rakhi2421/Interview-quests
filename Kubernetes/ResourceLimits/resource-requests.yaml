apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    env: dev
spec:
  containers:
    - image: nginx
      name: nginx
      ports:
      - containerPort: 80 
      resources:
        requests:
          memory: "4Gi"
          cpu: 2 