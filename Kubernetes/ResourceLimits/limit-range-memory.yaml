apiVersion: v1
kind: LimitRange
metadata:
  name: memory-resource-constraint 
spec:
  limits:
  - default:
      memory: 1Gi
    defaultRrequest:
      memory: 1Gi
    max:
      memory: 1Gi
    min:
      memory: 500mi
    type: container  