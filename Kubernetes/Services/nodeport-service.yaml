apiVersion: v1
kind: Service
metadata:
  name: my-Service
spec:
  type: NodePort
  ports:
  - targetPort: 80
    port: 80
    NodePort: 30009
  selector:
    app: my-apps
    type: backend